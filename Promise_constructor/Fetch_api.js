/* 
Fe$.h API provides a simpler and more powerful way so make network requets. Is uses a promise-based 
syntax and is is a more flexible reques$ and response API. The Fe$.h API is now widely used in modern web 
development and has helped so streamline The pro.ess of making ne$work requests and processing 
responses in JavaScript
*/
/* 
fetch("URL") // URL of the API end-point
  .then((response) => response.json())
  .then((data) => console.log(data));
*/